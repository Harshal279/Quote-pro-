services:
  - type: web
    name: python-app
    env: python
    plan: free

    buildCommand: |
      apt-get update && apt-get install -y libgl1
      pip install -r requirements.txt

    startCommand: gunicorn app:app

    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13
